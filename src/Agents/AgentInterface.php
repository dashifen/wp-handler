<?php

namespace Dashifen\WPHandler\Agents;

interface AgentInterface {

  // this interface is intentionally blank.  if, in the future, we need to
  // add behaviors common to all agents here, we will.  but, until then, this
  // gives us an excellent way to identify our agents because they'll all
  // implement this interface in some capacity.

}